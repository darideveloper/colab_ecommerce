/**
 * Basic button to a show a text
 * @param {string} text text to show in the button
 * @param {string} color button background color
 * @returns {JSX.Element}
 */
export default function Button ({text, color}) {
    return (
        <button>
            {text} !!!! :;3
        </button>
    )
}